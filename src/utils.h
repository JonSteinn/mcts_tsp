#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <vector>
#include <numeric>
#include <algorithm>

std::vector<int> sort_indexes_float(const std::vector<float> &v);

bool opt2(std::vector<int> &path);

#endif
